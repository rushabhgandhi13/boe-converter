# Copy this file to ".streamlit/secrets.toml" for local runs, or paste its
# contents into Streamlit Community Cloud: Manage app -> Settings -> Secrets.
#
# Do NOT commit the real secrets.toml (it is git-ignored).

app_password = "change-me-to-a-strong-password"
